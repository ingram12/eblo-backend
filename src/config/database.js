const config = {
    'connection': {
        'host': 'localhost',
        'user': 'root',
        'password': 'root',
    },
	'database': 'eblo',
  'users_table': 'users',
};

export default config;
